#define LED_PIN 11;
int brightness = 0;

void setup() {
  Serial.begin(9600);

  pinMode(LED_PIN, OUTPUT);

}

void loop() {
  brightness += 1;
  analogWrite(LED_PIN, brightness);
  Serial.println(brightness);
  delay(100);

}
